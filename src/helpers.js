export const vscoLink = photoId => {
  return `https://vsco.co/dylan-cheung/media/${photoId}`
}

export const githubLink = projectName => {
  return `https://github.com/dylancheung123/${projectName}`
}